describe('viewctrls', function () {
    var vc = $('<div>');

    // before('load basic widget instance', function() {
        // $.widget('moc.viewctrls', {});
    // });

    it('should initialize', function() {
        // vc.viewctrls();

        expect(vc).to.be.ok;
    });
});